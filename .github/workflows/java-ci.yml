# SPDX-FileCopyrightText: 2026 Bernard Ladenthin <bernard.ladenthin@gmail.com>
#
# SPDX-License-Identifier: Apache-2.0

name: Java CI

on:
  push:
    branches: [ main ]
    paths:
      - 'Java/**'
      - '.github/workflows/java-ci.yml'
  pull_request:
    paths:
      - 'Java/**'
      - '.github/workflows/java-ci.yml'
  workflow_dispatch:

jobs:
  # Discover every Java/<name>/ directory that has its own pom.xml.
  # Each module is a fully independent Maven project (no parent reactor), so
  # adding/renaming/removing one here needs no workflow edit - the matrix
  # below is generated dynamically from what's actually on disk.
  discover:
    runs-on: ubuntu-latest
    outputs:
      projects: ${{ steps.set-matrix.outputs.projects }}
    steps:
      - uses: actions/checkout@v7

      - id: set-matrix
        run: |
          projects=$(find Java -mindepth 2 -maxdepth 2 -name pom.xml \
            | sed -E 's#^Java/([^/]+)/pom\.xml$#\1#' \
            | sort \
            | jq -R -s -c 'split("\n") | map(select(length > 0))')
          echo "Discovered projects: $projects"
          if [ "$projects" = "[]" ]; then
            echo "::error::No Java/*/pom.xml found - discovery is broken or the tree is empty."
            exit 1
          fi
          echo "projects=$projects" >> "$GITHUB_OUTPUT"

  build-and-test:
    name: build-and-test (${{ matrix.project }})
    needs: discover
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        project: ${{ fromJson(needs.discover.outputs.projects) }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
          cache-dependency-path: Java/${{ matrix.project }}/pom.xml

      - name: Build and test ${{ matrix.project }}
        working-directory: Java/${{ matrix.project }}
        run: mvn --batch-mode --no-transfer-progress clean verify

      - name: Upload surefire reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: surefire-reports-${{ matrix.project }}
          path: Java/${{ matrix.project }}/target/surefire-reports/
          if-no-files-found: ignore
          retention-days: 14

  # Single required status check for branch protection - green only if every
  # discovered project's build-and-test job succeeded (fail-fast: false above
  # means a failing project doesn't cancel the others, so this job is what
  # actually gates the branch).
  all-green:
    name: All Java builds passed
    needs: build-and-test
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check matrix result
        run: |
          if [ "${{ needs.build-and-test.result }}" != "success" ]; then
            echo "::error::At least one Java module failed to build/test."
            exit 1
          fi
          echo "All Java modules built and tested successfully."
